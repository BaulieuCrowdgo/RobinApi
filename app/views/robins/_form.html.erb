<%= form_for(robin) do |f| %>
  <% if robin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(robin.errors.count, "error") %> prohibited this robin from being saved:</h2>

      <ul>
      <% robin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %>
    <%= f.check_box :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
